#include <stdio.h>
#include "lists.h"
#include <stdlib.h>

size_t list_len(const list_t *h)
{
	size_t list_len(str *);
	int count(str *);

	int count = 0;
	str *h = head;

	while (str != NULL)
	{
		count++;
		h = h->next;
	}
	return(count);
}
