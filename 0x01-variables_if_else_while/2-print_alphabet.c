#include <stdio.h>

/**
 * main - print alphabet in lower case
 *
 * Return: The printed alphabet
 */

int main(void)

{
	int c;

	for (c = 'a'; c <= 'z'; c++)

	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
