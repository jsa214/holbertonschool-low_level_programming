#include <stdio.h>

/**
 * main - print alphabet in lower case
 *
 * putchar with space cmd creates a n line
 *
 */

int main(void)

{
	int letter_c;

	for (letter_c = 'a'; letter_c <= 'z'; c++)

	{
		putchar(letter_c);
	}

	putchar('\n');

	return (0);
}
